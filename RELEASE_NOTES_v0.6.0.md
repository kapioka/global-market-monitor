# Release Notes v0.6.0

## 概要

v0.6.0 では、投資判断補助ツールとしての安全性、検証しやすさ、確認画面の見やすさを強化しました。

中心は、データ品質が悪い日に強い判断が出てしまうリスクを抑え、最新レポートと補足ダッシュボードで根拠を追いやすくすることです。

## 主な変更点

### 1. データ品質ガードを追加

- `reliability_policy.py` を追加
- sample-only は診断用として扱うように整理
- sample fallback が混ざる場合は `buy_window` を抑制
- 重要系列の欠損がある場合は `buy_window` を抑制
- live ratio が低い場合は action と confidence を安全側に制限
- proxy fallback のみの場合も confidence を抑制

### 2. レポートに信頼性情報を表示

- Markdown / HTML レポートに live ratio、fallback 件数、max action、confidence cap、cap reason を追加
- 補足ダッシュボードにもデータ品質ガードの状態を表示
- raw action と最終 action の違いを追いやすく改善

### 3. 補足ダッシュボードを追加

`supplement_dashboard.html` を、次の5画面へ分けて再構成しました。

- 履歴
- 判定
- セクター
- 市場監視
- 監査

最新レポートで市場の全体感を確認し、補足ダッシュボードで判定理由、履歴、監査情報を確認する流れを想定しています。

### 4. 実行構造を整理

- `pipeline.py` を追加し、データ取得からレポート payload 構築までの流れを分離
- `snapshot_store.py` を追加し、snapshot 保存と読込を分離
- `backfill.py` を追加し、履歴補完処理を分離
- `report_runtime.py` を追加し、レポート保存と履歴管理を分離
- `action_schema.py` を追加し、action label と cap 判定を共通化

### 5. 判定理由と表示文を整理

- `signal_messages.py` を追加し、スポット判断の日本語文言を分離
- `decision_attribution.py` を追加し、最終判断に効いた要因を JSON で追えるように追加
- reliability cap、blocker、recovery evidence、risk line などの寄与を見やすく整理

### 6. action validation を追加

- `action_validation.py` を追加
- `buy_window` / `watch` / `wait` 後の 4週、13週、26週、52週リターンを集計する基礎機能を追加
- `action_validation_report.py` で JSON / Markdown レポートを生成
- `run_action_validation.py` で保存済み履歴と価格 JSON から検証レポートを再生成可能に変更

### 7. 表示を改善

- セクターローテーション図を大きく表示
- 象限ラベルと色分けを調整
- 順位表との色の整合性を改善
- 危険ライン監視、市場監視、円建て・為替リスクの配置を調整
- 画面内に残っていた英語ラベルを、できるだけ日本語の表示へ寄せました

## このバージョンで改善されたこと

- サンプルデータや欠損データが混ざった日に、強い判断が出にくくなりました
- `buy_window` が購入指示ではなく、追加確認の候補状態であることを明確にしました
- 判断がデータ品質で降格された場合、その理由をレポートで確認しやすくなりました
- 大きくなっていた `main.py` の責務を分け、今後の保守がしやすくなりました
- 履歴、判定、セクター、市場監視、監査をタブで切り替えて確認できるようになりました
- 投資モデルとしての検証資料を作るための土台が入りました
- GitHub公開用に、動作するアプリ部分だけを切り出した構成を整理しました

## 注意点

- このツールは売買判断の自動化エンジンではありません
- `buy_window` は購入指示ではなく、追加確認の候補状態です
- action validation は、履歴と価格系列の品質に依存します
- 4週、13週、26週、52週の検証は日数ベースの近似です
- 税金、手数料、配当、実際の約定価格は検証に含みません
- 検証件数が少ない action は、平均リターンだけで判断しないでください
- 補足ダッシュボードは、実行後に生成される `project/reports/report_summary.json` と履歴データをもとに作られます
- 生成済みの HTML レポート、ログ、キャッシュは公開版には含めていません

## 今後の予定

- 実データ履歴とベンチマーク価格を使った局面別検証を追加
- 2018、2020、2022、2024-2025 などの市場局面別に誤判定を確認
- 重みやしきい値を変えたときの感度分析を追加
- action validation の結果を通常レポートや補足ダッシュボードへ統合するか検討
- 補足ダッシュボードの表示検証を継続
