# Release Notes v0.10.0

v0.10.0では、過去の市場警戒を読み返す「市場警戒年代記」を追加し、長期的なデータ保存に向けたSQLite移行を安全な照合段階で開始しました。

このリリースは、診断結果と根拠を分かりやすく残すための改善です。現在の売買判断を強めたり、自動売買へ接続したりするものではありません。

## 主な変更点

### 市場警戒年代記

- 通常実行時に、検証済みの根拠から年代記を生成または更新します。
- 補足ダッシュボードから別ウィンドウで開けます。
- 警戒局面と市場急落を、エピソード単位で時系列に読み返せます。
- 各エピソードには、価格チャート、当時の関連指標、警戒の進行、評価、データの出所を表示します。
- 関連指標は当時の証拠から選択し、ACWIを含め最大5系列まで比較できます。
- 目次、最近開いたタブ、検索、フィルターを使って長い履歴を移動できます。

### 市場データのSQLite保存

- 検証済みCSVから、利用者PC内のSQLiteへ市場時系列を初回移行できます。
- 系列、日付、値、欠損、件数がCSVと一致することを移行前後で確認します。
- 同じ入力を再実行しても重複を増やさず、`no_change`として扱います。
- 既存の無関係なDB、同時に作成された保存先、古い観測時刻による訂正は安全側で拒否します。
- 既存CSVは残し、現在も通常の読み取りに使用します。

### Risk Engine V2の証拠整合性

- official-series再生成比較を、通常のloaded-to-loaded比較として検証できるようにしました。
- replay/review、holdout/audit、coverage mismatchを比較合格の必須条件にしました。
- artifact freshnessへroot-cause成果物を追加しました。
- policy metadataが欠落した場合に、安全と推測せずfail-closedで停止します。
- 診断結果の改善はproduction decision logicへ接続していません。

### 公開前の安全性

- 公開用READMEを、用途、最短起動、画面の見方、データ制約が順に分かる構成へ整理しました。
- ローカル市場データ、生成済みレポート、SQLite、ログを公開ソースから除外しました。
- 依存監査で検出されたSoupsieveのDoS脆弱性を避けるため、lockを2.8.4へ更新しました。
- tag pushでもCIを実行し、検証済みsource archiveをActions artifactとして保存できるようにしました。
- 個人・非商用の実行だけを許可し、商用利用、改変、再配布を禁止する利用条件を明記しました。
- pandas 2と3の両方で、通常の欠損値は保持し、無限値は拒否するSQLite移行検証を確認しました。

## 使う人が見るポイント

- `run_main.bat`で通常実行した後、補足ダッシュボードから「市場警戒年代記」を開けます。
- 年代記は過去の警戒を検証する画面です。現在の購入・売却を指示するものではありません。
- 必要な証拠が不足、不整合、期限切れの場合は、古い年代記を最新として更新しません。
- SQLiteはアプリ内部で使用します。利用者によるSQL操作は不要です。
- SQLite移行後も既存CSVは削除されません。

## 変更していないこと

- `final_action`の決定ロジック
- productionの`buy_readiness_score`
- `reliability_policy`
- active / proposed threshold JSON
- buy-window / buy-candidate policy
- Hindenburg Omenの表示専用境界
- 自動売買や注文機能

## 公開版に含めないもの

- 取得済みの市場キャッシュ
- 利用者PC内のSQLite DB
- 生成済みHTML・Markdownレポート
- 実行ログ、一時ファイル、手動入力データ
- ローカル移行の件数、時刻、ハッシュを記録した実績manifest

ソース配布物は追跡済みファイルだけから作成し、除外対象と秘密情報の検査後に公開します。

## ライセンス

本ソフトウェアはオープンソースではありません。個人による私的かつ非商用の実行に限って利用できます。詳しい条件、無保証、責任制限は`LICENSE`を確認してください。
