# Release Notes v0.11.0

v0.11.0では、最新レポートと補足ダッシュボードの要素を削らずに再配置し、画面幅に応じて自然に読み進められる構成へ更新しました。保存済みの市場警戒年代記は公開日数にかかわらず、いつでも読み返せます。

このリリースは表示、閲覧継続性、公開前セキュリティの改善です。現在の売買判断を強めたり、自動売買へ接続したりするものではありません。

## 主な変更点

### レポートの視認性と導線

- 広い画面では最新レポートと補足要約を約58:42で並べます。
- 並べきれない画面幅では補足を最新レポートの下へ回し、モバイルでは1列にします。
- 「今日の判断 → 観察候補 → セクター概要 → 危険ライン・補助確認 → 履歴」の順序を維持します。
- 補足ダッシュボードは広幅で詳細を2列にし、狭い画面では1列へ切り替えます。
- 既存の確認項目と10個の補足セクションは削除していません。

### セクター概要

- 4象限のセクターローテーション図をセクター概要へ戻しました。
- 2週間前と先週を薄い円、現在を強調した円で示します。
- 2週間前から先週、先週から現在までの2本の移動軌跡で方向変化を確認できます。
- 相対モメンタムを右上へ配置し、セクター別の横棒をその下へ整理しました。

### 市場警戒年代記

- 保存済みの有効な全エピソードを、経過日数にかかわらず常時閲覧できます。
- 根拠不足、不整合、期限切れは新しいエピソードへの更新だけを停止します。
- 鮮度条件を理由に過去のエピソードを非公開にする仕組みは使用しません。

### 公開前の安全性

- 履歴ダッシュボードと市場警戒年代記の埋め込みJSONを、script終了文字列から保護しました。
- 動的なHTML断片、属性、生成日時を文脈に応じてエスケープします。
- GitHub Actionsへ読み取り専用の権限を明示し、checkoutの認証情報保持を無効化しました。
- 外部Actionを検証済みの完全なコミットSHAへ固定しました。
- Gitleaksを必須のCI検査に変更しました。
- 公開文書から利用者名を含むローカル絶対パスを削除しました。

## 変更していないこと

- `final_action`の決定ロジック
- productionの`buy_readiness_score`
- `reliability_policy`
- active / proposed threshold JSON
- buy-window / buy-candidate policy
- Risk Engine V2のshadow・診断専用境界
- Hindenburg Omenの表示専用境界
- 自動売買や注文機能

## 公開版に含めないもの

- 取得済みの市場キャッシュ
- 利用者PC内のSQLite DB
- 生成済みHTML・Markdownレポート
- 実行ログ、一時ファイル、手動入力データ
- ローカル比較画像と環境固有パス

ソース配布物は追跡済みファイルだけから作成し、除外対象、秘密情報、依存関係、manifestを検査してから公開します。

## ライセンス

対象と利用条件は従来から変更していません。本ソフトウェアはオープンソースではなく、個人による私的かつ非商用の実行に限って利用できます。詳しい条件、無保証、責任制限は`LICENSE`を確認してください。
